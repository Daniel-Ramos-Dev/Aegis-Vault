services:
  - type: web
    name: aegis-vault
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: aegis-db
          property: connectionString

databases:
  - name: aegis-db
    plan: free
